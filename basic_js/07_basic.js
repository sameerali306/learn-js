// method of the string
let word="Derogate"
const word2=word.substring(0,5)/*the both substring and slice are use to get a specific portion of word from a string
// console.log(word2);            but in slice we give negative value as not do in substring*/
const word3=word.slice(-6,-1)
// console.log(word3);

let name="   pakistan   "   /*the trim remove the white space from the variable on both side */
// console.log(name);

// console.log(name.trim());

let fname="sameer ali"       /*the replace method of string is use to replace particular word fron anather word or alphebt */
console.log(fname);
let fname2=fname.replace("ee","i")
console.log(fname2);

console.log(fname.includes("ali"));  /*by includes method we can find the specific word or letter is present in string or not*/








